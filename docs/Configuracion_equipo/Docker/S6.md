# S6 – Servidor de Activos Estáticos (Static)

## Objetivo de S6
Configuramos el servidor S6 utilizando una imagen de NGINX sobre Alpine para centralizar todos los recursos que definen el aspecto visual de Extagram. Su función es servir archivos que no cambian con el uso de la aplicación, como la hoja de estilos CSS, logotipos, iconos y el archivo de previsualización SVG.

Al separar el diseño (S6) del contenido subido por los usuarios (S5), garantizamos una organización profesional de los activos y permitimos que el navegador cachee estos archivos de forma más eficiente.

## Configuración y Funcionamiento de S6
- **Despliegue del Contenedor:** Al igual que S5, este contenedor funciona en modo read-only (:ro), ya que su contenido es estático y solo debe ser modificado por el administrador del sistema al actualizar el diseño de la web.
  ```sql
  # Comando de ejecución manual
  docker run -d \
    --name s1-s1-s6-static-1-1 \
    --network red_entrada \
    --network red_servicios \
    -v /home/ec2-user/sprint2/S6/static:/usr/share/nginx/html:ro \
    nginx:alpine
  ```
- **Estructura del contenedor:**
  ```sql
  S6/
  └── static/
      ├── style.css      # Definición de colores, fuentes y layout
      └── preview.svg    # Icono predeterminado del formulario de subida    
  ```
  | Fichero | Función Técnica |
  |--------|------|
  | style.css | Hoja de Estilos: Define la estética "dark mode" de Extagram y el diseño responsivo. |
  | preview.svg | Asset Visual: Recurso gráfico ligero que sirve de guía visual en el formulario de publicación. |  

- **Contenido de ficheros:**
  - **style.css:**
    ```sql
    :root {
    --bg-color: #0f172a;
    --card-bg: #1e293b;
    --accent: #38bdf8;
    --text-main: #f1f5f9;
    --text-muted: #94a3b8;
    --header-height: 70px;
    }
    
    * {
        box-sizing: border-box;
    }
    
    body {
        background-color: var(--bg-color);
        font-family: 'Segoe UI', system-ui, sans-serif;
        color: var(--text-main);
        margin: 0;
        /* Evita que el contenido se esconda bajo el header fixed */
        padding-top: calc(var(--header-height) + 20px); 
        padding-bottom: 50px;
    }
    
    /* --- CABECERA (Header) --- */
    .main-header {
        background: rgba(15, 23, 42, 0.9);
        backdrop-filter: blur(12px);
        border-bottom: 1px solid #334155;
        height: var(--header-height);
        width: 100%;
        display: flex;
        justify-content: center;
        align-items: center;
        position: fixed;
        top: 0;
        left: 0;
        z-index: 1000;
    }
    
    .main-header h1 {
        margin: 0;
        font-size: 1.8rem;
        font-weight: 800;
        background: linear-gradient(45deg, var(--accent), #a78bfa);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        text-transform: lowercase;
    }
    
    /* --- CONTENEDOR PRINCIPAL --- */
    .main-container {
        width: 95%;
        max-width: 550px;
        margin: 0 auto;
        display: flex;
        flex-direction: column;
    }
    
    /* --- FORMULARIO Y CARGA --- */
    form {
        background: var(--card-bg);
        padding: 20px;
        border-radius: 16px;
        border: 1px solid #334155;
        display: flex;
        flex-direction: column;
        gap: 15px;
        margin-bottom: 30px;
    }
    
    input[type=text] {
        background: #0f172a;
        border: 1px solid #334155;
        border-radius: 8px;
        padding: 12px;
        color: white;
        width: 100%;
        outline: none;
    }
    
    input[type=text]:focus {
        border-color: var(--accent);
    }
    
    /* Esta es la zona punteada que reemplaza al botón feo */
    label[for="file"] {
        border: 2px dashed #475569;
        border-radius: 12px;
        padding: 30px;
        text-align: center;
        cursor: pointer;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        color: var(--text-muted);
        transition: all 0.2s ease;
    }
    
    label[for="file"]:hover {
        border-color: var(--accent);
        background: rgba(56, 189, 248, 0.05);
    }
    
    #preview {
        max-width: 120px;
        max-height: 120px;
        object-fit: contain;
        border-radius: 8px;
        margin-bottom: 10px;
        opacity: 0.8;
    }
    
    input[type=submit] {
        background: var(--accent);
        color: #0f172a;
        font-weight: bold;
        border: none;
        border-radius: 8px;
        padding: 14px;
        cursor: pointer;
        transition: 0.3s;
        font-size: 1rem;
    }
    
    input[type=submit]:hover {
        background: #7dd3fc;
        transform: translateY(-2px);
        box-shadow: 0 4px 12px rgba(56, 189, 248, 0.3);
    }
    
    /* --- FEED DE POSTS --- */
    .post {
        background: var(--card-bg);
        border-radius: 16px;
        border: 1px solid #334155;
        overflow: hidden;
        margin-bottom: 25px;
        box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
    }
    
    .post-image img {
        width: 100%;
        height: auto;
        display: block;
        border-bottom: 1px solid #334155;
    }
    
    .post p {
        padding: 15px;
        margin: 0;
        line-height: 1.5;
        font-size: 1.05rem;
    }
    
    .post p strong {
        color: var(--text-main);
    }
    ```
    
